# Swig 0.29.4 release notes

## GitHub Release publication

- Validated CLI artifacts are published with `softprops/action-gh-release@v3` instead of a handwritten `gh release` shell block.
- Publication now fails explicitly if the downloaded release-artifact glob is empty.

No analysis method, default, or output schema changed from 0.29.3.
